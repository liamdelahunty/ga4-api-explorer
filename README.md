# GA4 API Explorer

This project is for exploring the Google Analytics 4 (GA4) API.

## Getting Started

1.  Install dependencies: `pip install -r requirements.txt`
2.  Configure your API credentials in the `config` directory.
3.  Run the desired script.
